Fenced with info words:

```rust ignore should-be-dropped
let x = "only first word is the class";
```

~~~~
tilde fence with ``` inside
~~~~

Unclosed at end of a quote:

> ```
> fenced in quote

Inline `` code with `backticks` `` and `a  double  spaced  span`.

<table>
  <tr><td>raw table</td></tr>
</table>

Text with <span class="x">inline raw</span> and a bare < sign, plus
self-closing <br/> and <!-- inline comment -->.

<?php instruction ?>

<![CDATA[ character data ]]>

Final fence unclosed at EOF:

```text
no closing fence
