1. Outer ordered item

   > A quote inside a list item.
   >
   > 1. An ordered list inside the quote
   > 2. With two items

   ```python
   def nested():
       return "code in list"
   ```

2. Second outer item

   - Inner bullet
     1. Deeper ordered
        - Deepest bullet with *emph in **strong** text*

- Item containing

  two paragraphs

- Item with trailing code:

      indented code in loose list

> Quote containing
>
> ---
>
> a thematic break and a [link with *emph* inside](x.html).
