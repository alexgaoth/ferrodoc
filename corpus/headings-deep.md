## A document whose shallowest heading is a second level

Numbering counts from the shallowest heading in the document, not from
level one, so every number here has **one** component until a deeper
heading appears. A file that opens at `#` numbers the same headings
differently, which is why that case is a separate document.

#### Straight to a fourth level

A jump opens exactly one level of nesting in the contents, not two.

### Back to a third

## Same name

## Same name

Two headings with one name: the identifiers are uniqued and the contents
must follow the identifiers rather than the text.

### *Emphasis*, `code` and <b>raw HTML</b> in a heading

An entry keeps the heading's inline markup.
